// Adapter to maintain existing import paths
// Re-export ProfileContext under Auth names for compatibility
export { ProfileContext as AuthContext, ProfileProvider as AuthProvider } from "./ProfileContext";
